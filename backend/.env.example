# Use MongoDB (SD_IoT) for lights. Without this, the app uses SQLite.
# Get the connection string from Atlas: Connect → Drivers → Python.
# Replace <password> with your database user password.
MONGODB_URI=mongodb+srv://michael_user1:<password>@seniordesignbudderfly.4xcvxbi.mongodb.net/SD_IoT
MONGODB_DB_NAME=SD_IoT
